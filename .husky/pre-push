#!/bin/sh
# Check CI status before push
echo "🔍 Checking CI status before push..."

# Check if gh CLI is available
if ! command -v gh >/dev/null 2>&1; then
  echo "⚠️  GitHub CLI not installed - skipping CI check"
  exit 0
fi

# Get recent CI status
CI_STATUS=$(gh run list --limit 1 --json conclusion,name 2>/dev/null)

if [ -n "$CI_STATUS" ] && [ "$CI_STATUS" != "[]" ]; then
  # Check for failures
  if echo "$CI_STATUS" | grep -q '"conclusion":"failure"'; then
    echo "❌ CI is currently failing!"
    echo ""
    if command -v jq >/dev/null 2>&1; then
      echo "$CI_STATUS" | jq -r '.[] | "   Failed: \(.name)"'
    else
      echo "   (Install jq for detailed failure info)"
    fi
    echo ""
    echo "⚠️  Push blocked due to CI failures"
    echo "   Use 'git push --no-verify' to override (not recommended)"
    echo "   Or fix the CI issues first"
    exit 1
  else
    echo "✅ CI is passing - proceeding with push"
  fi
else
  echo "ℹ️  No CI runs found or unable to check - proceeding with push"
fi

exit 0